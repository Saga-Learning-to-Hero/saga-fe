# Deprecated — không dùng trong luồng production

Thư mục này giữ UI minh họa cũ (mock dự án nhóm, chọn trưởng nhóm local). Route hiện tại dùng:

- `src/features/lecturer/teams` cho danh sách nhóm, đổi trưởng nhóm và chuyển thành viên
- `src/features/lecturer/contribution` cho trọng số dự án nhóm và đánh giá đóng góp

Không import lại các component trong thư mục này vào `src/app` hay service API.
